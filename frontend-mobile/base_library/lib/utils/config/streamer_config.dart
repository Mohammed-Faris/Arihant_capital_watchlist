class StreamerConfig {
  static String socketHostUrl = '';
  static String socketMode = '';
  static int socketHostPort = 0;
  static bool binaryStream = false;
  static Duration socketTimeout = const Duration(milliseconds: 2000);
}
